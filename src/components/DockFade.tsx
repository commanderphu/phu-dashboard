export default function DockFade() {
    return (
        <div
            className="pointer-events-none fixed bottom-0 left-0 right-0 h-24
                 bg-gradient-to-t from-bg/95 via-bg/70 to-transparent"
        />
    );
}
